-- Seeded by rimeterm on first launch. Safe to edit; rimeterm never
-- overwrites this file. Delete `~/.rimeterm/yazi/init.lua` to re-seed
-- it on next start.
--
-- Loads the rimeterm-bridge plugin so Alt+V and yazi's `hover`/`cd`
-- events reach rimeterm's OSC 1337 channel.
require("rimeterm-bridge"):setup()
