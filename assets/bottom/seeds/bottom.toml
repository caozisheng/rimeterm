# Seeded by rimeterm on first launch. Safe to edit; rimeterm never
# overwrites this file. Delete `~/.rimeterm/bottom/bottom.toml` to
# re-seed it on next start.
#
# See https://clementtsang.github.io/bottom/nightly/configuration/config-file/
# for the full schema. Values below are safe defaults.

[flags]
temperature_type = "celsius"
rate = "1s"
